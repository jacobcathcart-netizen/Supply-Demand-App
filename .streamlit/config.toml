[theme]
primaryColor = "#1892DF"          # Light Blue — buttons, links, active widgets
backgroundColor = "#FFFFFF"        # White background
secondaryBackgroundColor = "#F0F2F6"  # Light gray for sidebar / containers
textColor = "#0A3370"             # Dark Navy — all body text
font = "sans serif"               # Tahoma fallback (closest Streamlit option)
